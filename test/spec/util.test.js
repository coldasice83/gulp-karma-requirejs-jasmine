/**
 * Created by n3927 on 2015-04-17.
 */
define(['underscore'], function(_) {
  describe('just checking', function() {
    it('works for underscore', function() {
      // just checking that _ works
      expect(_.size([1,2,3])).toEqual(3);
    });
  });
});
